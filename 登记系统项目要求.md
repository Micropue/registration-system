# 登记系统项目要求

## 一、简介

本系统是一套多角色的业务登记与订单管理平台：

- **普通用户**：填写并提交登记订单（支持动态表单、图片上传、余额扣减），跟踪处理进度，与管理员聊天沟通，提交工单、充值申请、每日报告。
- **管理员 / 超级管理员**：按 APP 分类处理订单、审核充值、处理工单、管理公告、查看日报统计；通过账户组、下属层级与余额委托组织运营体系。

系统需同时适配 PC 与手机浏览器，并支持以 PWA 方式安装到桌面使用。

## 二、技术栈

| 层 | 要求 |
| --- | --- |
| 前端 | Vue 3 + Vite + Vuetify（组件库）+ Pinia + Vue Router，TypeScript |
| 后端 | Python 3.10+ / FastAPI，原生 SQL 访问 MySQL（不使用 ORM） |
| 数据库 | MySQL 8.0，utf8mb4 |
| 实时通信 | WebSocket（通知、订单聊天、聊天室） |
| 部署 | Docker Compose + Nginx，支持静态前端与 API 反向代理 |
| PWA | `vite-plugin-pwa`，支持安装与版本强制更新 |

## 三、前端规范

1. 界面支持响应式尺寸变化，手机端与电脑端内容不错位、可正常使用。
2. PWA：使用 Vite PWA 插件，注册策略为 `prompt`；检测到新版本时强制提示更新，更新完成前禁止继续操作。
3. 所有接口地址统一定义在 `config/api-url.ts`，使用枚举导出。
4. 所有接口请求体与响应体类型统一定义在 `config/api-type.ts` 并导出。
5. 请求必须封装统一的 `ajax()` 函数，支持泛型以提供类型提示；表单类请求通过 `isFormData` 走 `multipart/form-data`。
6. 页面目录与访问路径对应，每个页面使用 `index.vue`；路由集中在 `router/index.ts` 并通过守卫做登录/权限校验。
7. 界面简洁规范，采用应用主色（红色系）与左右分栏布局；列表页统一使用 `AppDataTable` 组件（分页、搜索、筛选、拖拽排序）。
8. 提示统一使用 Snackbar，禁止使用 `alert()`。

## 四、后端规范

### 4.1 返回结果规范

每个接口必须返回统一结构：

```json
{ "code": 200, "msg": "successfully send", "data": [] }
```

| 名称 | 作用 | 类型 | 必须 |
| --- | --- | --- | --- |
| `code` | 状态码，与 HTTP 响应码一致 | Int | 是 |
| `msg` | 状态信息，以英文为主 | String | 是 |
| `data` | 返回数据 | any[] \| object \| null | 是 |

### 4.2 鉴权与权限

1. 所有受保护接口需携带 `Authorization: Bearer <token>`；token 由登录接口签发并写入数据库会话表，移除会话即失效。
2. 权限模型为「模块 → 操作」两级树，挂在账户组上；超级管理员组始终拥有全部权限。
3. 查看类权限区分数据范围（下属 / 其他），后端查询时按权限自动过滤。
4. 所有权限校验统一通过 `require_perm(authorization, "模块", "操作")` 完成。

### 4.3 数据库

**账户数据**

1. 账户使用唯一 UID（保留自增 `id` 字段，业务关联一律使用 UID）。
2. 创建账户时用户名可重复；密码 6–16 位，仅允许字母、数字、短横线。
3. 记录内容包含：用户名、密码（SHA-256）、注册时间、上次登录时间、上次登录设备、登录会话 token 列表。
4. 登录会话只存 token，不存用户名或用户 UID；token 被移除则登录失效。

**登记数据**

1. `registrations.data` 用 JSON 存储订单字段，其中业务应用名称使用保留键 `应用`。
2. 各 APP 的字段结构由「模板」定义，模板支持版本管理与增删改，字段类型包括：文本、长文本、数字、日期、时间、日期时间、下拉、单选、多选、图片、数值区间、日期区间、时间区间。
3. 表结构在后端启动时自动创建与迁移，禁止手工执行 SQL。

## 五、功能要求

### 5.1 账户与权限

1. 普通账户不可自行注册，只能由管理员创建；管理员账户由超级管理员创建。
2. 系统默认存在一个超级管理员账户（`admin` / `admin-123456`），上线后需立即修改密码。
3. 后台支持账户增删改查、重置密码、强制下线。
4. 支持账户组管理：新建/修改/删除账户组，按权限树勾选权限；修改权限后立即生效。
5. 支持下属管理：建立上级-下级层级关系，上级可在权限范围内查看、处理下属的数据。
6. 支持余额委托：为「用户 × APP」建立余额扣除链接，消费改为从上级余额层层扣除；已委托用户的余额查看与充值申请功能禁用。

### 5.2 APP 与模板配置

1. 支持 APP 的新增、修改、删除、拖拽排序与 Excel 批量导入。
2. APP 字段包含：名称、备注、图标、强调色、余额类型（公里数 / 次数 / 未设置）、余额扣除方式（四舍五入 / 五舍六入）。
3. 每个 APP 支持多版本模板；模板字段支持必填、默认值、选项、多图、详情样式（加粗/颜色/字号）、复制按钮、强调弹窗条件与内容。
4. 模板支持复刻，便于快速创建新版本。

### 5.3 登记订单

1. 用户选择 APP → 选择模板版本 → 输入数量（启用余额时）→ 填写动态表单 → 上传图片 → 提交订单。
2. 提交时校验并扣减用户余额（或委托上级余额），余额不足不允许提交。
3. 订单支持优先级（低/中/高）、进度状态（未处理/已处理/已驳回）与驳回原因。
4. 管理员可按「全部 / 按 APP」分栏处理，支持通过、驳回（可自定义退回数量）、修改字段、删除、转发到聊天室。
5. 订单被处理或驳回后进入「二次订单」，用户可修改并重新提交；重新提交执行「先退回原扣除、再按新数量扣除」。
6. 订单列表支持搜索、筛选、排序、分页；详情支持图片预览与一键导出。
7. 修改订单时若旧表单值与当前模板不一致，需提示「无法恢复原状态，模板与原模板不一致」。

### 5.4 余额与充值

1. 每个用户 × 每个 APP 独立余额，单位按 APP 的余额类型显示（公里 / 次）。
2. 用户可查看自己的各 APP 余额及流水；管理员可查看全部流水与指定 APP 的用户余额并手动增减。
3. 余额变动需记录流水：类型、数量、变动后余额、关联订单、备注、时间。
4. 用户可提交充值申请，管理员审批（通过/驳回并填写原因），审批结果通知申请人。

### 5.5 工单

1. 用户提交反馈（标题 + 内容，内容 0–100 字），必须登录后使用。
2. 用户与管理员可多轮回复；管理员可标记解决、驳回或删除。
3. 处理完成后用户端显示「已处理」状态。

### 5.6 公告

1. 管理员可新建、编辑、发布、下架、删除公告。
2. 已发布的最新公告在用户进入应用时以弹窗展示；被用户关闭后不再重复弹出（按公告 UID 记录）。
3. 有「公告管理.查看」权限的用户可在公告页查看全部公告。

### 5.7 通知与待处理任务

1. 站内通知支持未读计数、单条/按关联/按类型/全部已读。
2. 管理端顶部显示待处理任务（订单、工单、充值），实时更新角标。
3. 通过 WebSocket 实时推送通知变化、业务数据变化与聊天未读。

### 5.8 聊天

1. 订单聊天：用户与管理员针对单个订单沟通，支持文本、图片、撤回；消息触发订单回到「未处理」并产生未读提醒。
2. 全局聊天室：支持文本、图片、撤回、置顶、在线用户列表；订单可转发到聊天室。
3. 聊天权限独立控制（`聊天` / `聊天室`）。

### 5.9 日报

1. 管理员可配置日报字段（类型、必填、选项、排序）。
2. 用户按配置填写并提交每日报告，同一天只保留一份（可修改）。
3. 管理员可查看日报列表、按日期统计、导出 Excel。

### 5.10 更新日志与版本

1. 更新日志由构建脚本从 git 提交历史自动生成，管理员可在后台查看。
2. 前端构建产出版本号，PWA 检测到新版本后强制更新。

## 六、示例登记字段

以下为可通过后台模板自由配置的示例字段（仅作参考，非固定表单）：

| 命题 | 类型 | 必填 | 其他要求 |
| --- | --- | --- | --- |
| 人脸识别 | boolean | 是 | |
| 业务应用 | 列表 | 是 | 等待后台录入 |
| 所属单位 | 列表 | 是 | 等待后台录入 |
| 所在区域 | string | 否 | 用户自行填写 |
| 是否首次登记 | boolean | 是 | |
| 手机号 | string | 是 | |
| 密码 | string | 是 | |
| 每日数量区间 | int - int | 是 | 使用区间，最低-最高 |
| 单次最低数值 | float | 是 | |
| 单次最高数值 | float | 是 | |
| 登记数值需求 | float | 是 | |
| 精确时间段 | time - time | 否 | 使用区间 |
| QQ号 | string | 是 | 包含正则验证 |
| 微信号 | string | 是 | |
| 备注 | string（0–500） | 否 | |
