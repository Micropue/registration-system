# 2026-5-23多啦A梦系统架构修改执行方案

本篇文章是关于系统架构更改的设计与实施方案。当前系统的部分逻辑可能需要废弃。在编码结束后，你需要告诉我你咋后端部分修改的内容，数据库架构做了哪些变化。

> [!CAUTION]
>
> 一定要多注意后端的接口，在修改别的接口逻辑时不要影响其他的接口，因为平台的功能现在非常复杂，我没办法做到完整测试

## 实施计划

1. 将原有的`/`根目录访问页面修改为用于显示通知的界面，不分用户，无论哪种用户都要显示通知。用来显示所有通知信息，可以加入懒加载。点击对应的通知需要跳转到对应的界面或者打开对应的弹窗。
2. 右上角通知中心部分，通知中心仅显示未读的通知，已读的通知仅显示3天的。
3. 余额部分功能需要大改：
   1. 改成每个用户每个App都有余额，无论是次数还是公里数。（注意在用户组的权限配置中也要加入这个选项）
   2. 每个用户每个App的余额可以被查看，在左侧边栏新增“余额查看”功能区，让有这个权限的用户查看到自己每个App的余额。（注意用户组也要加入这个权限配置），以表格的形式显示，使用`AppDataTable`组件实现，并且支持翻页与筛选。每个App要包含的操作有：“充值申请”、“查看流水”（注意要与余额流水的显示模式一致，要显示与其一样详细的信息）
   3. 左侧边栏余额流水部分依旧需要保留，里面的App一列需要加入App图标。但是注意是每个有这个权限的用户都需要有这个功能区。并且只显示这个用户的，而不是显示所有的流水。
   4. 在超级管理员的跑步App配置内，删除“余额”一栏，操作按钮加一个“余额管理”，点击进去新增一个界面用来显示这个App每个用户的余额，注意也要支持分页功能因为用户可能会很多。与之前的余额管理逻辑一样，可以设置余额模式与为用户手动充值或减少跑量。与其不同的是余额模式设置是在跑步App配置界面，为表格新增余额类型一栏，在表格的余额类型一栏进行设置。
   5. 余额管理界面每行都要包含的操作：“增加余额”，“减少余额”。并且实现该功能。
   6. 对于订单处理后的余额基本逻辑一致，只不过是扣掉用户自己的余额而不是全局。当然跑量依旧需要显示。