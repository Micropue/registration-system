# 二次订单标记逻辑修改方案

## 需求背景

基于图片中橙色头像人物（产品经理/开发者）的需求，需要修改系统中二次订单的标记逻辑。

### 当前逻辑问题
- **当前实现**：只要用户重新提交订单（无论当前状态如何），都会设置 `is_secondary = 1`
- **问题**：当订单状态还是 `pending`（员工未处理）时，用户修改订单也会被标记为二次订单

### 期望逻辑
- **期望实现**：只有当员工已经处理过订单（状态不是 `pending`）后，用户修改订单才应该标记为二次订单
- **具体场景**：
  1. 订单状态为 `pending`（员工未处理） → 用户修改 → **不标记**为二次订单
  2. 订单状态为 `approved` 或 `rejected`（员工已处理） → 用户修改 → **标记**为二次订单

## 代码分析

### 关键文件位置
1. **后端订单逻辑**：`server/src/account_service.py`
2. **后端API端点**：`server/src/main.py`
3. **用户端订单页面**：`app/src/pages/sign/index.vue`
4. **管理员订单页面**：`app/src/pages/admin/registers/index.vue`

### 当前实现分析

#### 1. `resubmit_registration` 函数（用户重新提交订单）
- **位置**：`account_service.py` 第1352-1394行
- **问题**：第1370行直接设置 `is_secondary = 1`，没有检查当前订单状态

#### 2. `update_registration_status` 函数（管理员处理订单）
- **位置**：`account_service.py` 第1531-1567行
- **逻辑**：当状态变回pending时，如果 `process_count > 0`，设置 `is_secondary = 1`
- **分析**：此逻辑已基本符合需求

#### 3. 前端显示逻辑
- **管理员界面**：`admin/registers/index.vue` 第122行显示"待二次处理"标签
- **用户界面**：没有显示二次订单标签

## 修改方案

### 1. 后端代码修改

#### 修改函数：`resubmit_registration`

**当前代码**（第1370行）：
```python
cursor.execute("UPDATE registrations SET data = %s, create_time = %s, status = 'pending', reject_reason = NULL, priority = %s, template_uid = %s, amount = %s, is_secondary = 1 WHERE uid = %s", (json.dumps(data, ensure_ascii=False), now, priority, template_uid, amount, uid))
```

**修改为**：
```python
# 根据订单当前状态决定是否设置is_secondary
if old_status and old_status != 'pending':
    # 订单已处理过（approved或rejected），设置is_secondary = 1
    cursor.execute("UPDATE registrations SET data = %s, create_time = %s, status = 'pending', reject_reason = NULL, priority = %s, template_uid = %s, amount = %s, is_secondary = 1 WHERE uid = %s", (json.dumps(data, ensure_ascii=False), now, priority, template_uid, amount, uid))
else:
    # 订单未处理（pending），不设置is_secondary
    cursor.execute("UPDATE registrations SET data = %s, create_time = %s, status = 'pending', reject_reason = NULL, priority = %s, template_uid = %s, amount = %s WHERE uid = %s", (json.dumps(data, ensure_ascii=False), now, priority, template_uid, amount, uid))
```

**修改说明**：
- 在更新订单状态前，检查 `old_status`（订单当前状态）
- 只有当 `old_status` 不是 `'pending'` 时，才设置 `is_secondary = 1`
- 如果 `old_status` 是 `'pending'`，则不修改 `is_secondary` 字段

### 2. 数据库兼容性处理

**考虑点**：现有数据可能已经有 `is_secondary = 1` 的订单，修改逻辑后不会影响这些数据。

**结论**：不需要数据库迁移，因为：
1. 修改只影响新提交的订单
2. 现有数据的 `is_secondary` 值保持不变
3. 逻辑变更向后兼容

### 3. 前端代码（无需修改）

**分析**：前端代码只是显示 `is_secondary` 字段的值，没有设置逻辑。

**相关代码**：
- `app/src/pages/admin/registers/index.vue` 第122行：显示"待二次处理"标签
- `app/src/pages/sign/index.vue`：用户端没有显示二次订单标签

**结论**：前端无需修改，后端逻辑修改后会自动生效。

## 测试方案

### 测试场景1：用户修改未处理订单
1. 用户创建新订单（状态：pending）
2. 用户修改订单内容
3. **预期结果**：订单 `is_secondary` 保持为0或NULL

### 测试场景2：用户修改已处理订单
1. 用户创建新订单（状态：pending）
2. 管理员处理订单（状态变为approved或rejected）
3. 用户修改订单内容
4. **预期结果**：订单 `is_secondary` 变为1

### 测试场景3：多次修改测试
1. 订单状态为pending，用户多次修改 → 都不应标记为二次订单
2. 订单状态为approved，用户多次修改 → 都应标记为二次订单

### 测试场景4：状态流转测试
1. 订单状态：pending → approved → pending → approved
2. 用户在第二次pending状态修改 → 应该标记为二次订单（因为process_count > 0）

## 实施步骤

1. **备份数据库**：修改前备份 `registrations` 表
2. **修改后端代码**：按上述方案修改 `account_service.py`
3. **重启后端服务**：`docker compose restart server`
4. **执行测试**：按照测试方案验证功能
5. **监控日志**：观察是否有异常错误

## 风险评估

### 低风险
- 修改范围小，只涉及一个函数的逻辑
- 不影响现有数据
- 向后兼容
- 前端无需修改

### 注意事项
- 确保 `old_status` 变量不为None
- 考虑并发修改的情况（当前系统似乎没有锁机制）

## 验证方法

修改完成后，可以通过以下方式验证：
1. 创建新订单，修改后检查 `is_secondary` 字段
2. 处理订单后再次修改，检查 `is_secondary` 字段
3. 查看管理员界面是否正确显示"待二次处理"标签

## 相关代码片段

### 当前 `resubmit_registration` 函数关键部分
```python
def resubmit_registration(self, uid: str, user_uid: str, data: dict[str, Any], priority: str = 'low', template_uid: str = '', amount: float | None = None) -> None:
    now = self.db.now()
    old_amount = None
    old_app_name = None
    old_status = None
    with self.db.connect() as connection:
        cursor = connection.cursor(dictionary=True)
        cursor.execute("SELECT amount, status, JSON_UNQUOTE(JSON_EXTRACT(data, '145应用')) as app_name FROM registrations WHERE uid = %s", (uid,))
        reg = cursor.fetchone()
        if reg:
            old_amount = reg.get("amount")
            old_app_name = reg.get("app_name")
            old_status = reg.get("status")
        # ... 其他代码 ...
        cursor.execute("UPDATE registrations SET data = %s, create_time = %s, status = 'pending', reject_reason = NULL, priority = %s, template_uid = %s, amount = %s, is_secondary = 1 WHERE uid = %s", (json.dumps(data, ensure_ascii=False), now, priority, template_uid, amount, uid))
        connection.commit()
```

### 修改后的逻辑
```python
# 在获取old_status后，修改UPDATE语句
if old_status and old_status != 'pending':
    # 订单已处理过，设置is_secondary = 1
    cursor.execute("UPDATE registrations SET data = %s, create_time = %s, status = 'pending', reject_reason = NULL, priority = %s, template_uid = %s, amount = %s, is_secondary = 1 WHERE uid = %s", (json.dumps(data, ensure_ascii=False), now, priority, template_uid, amount, uid))
else:
    # 订单未处理，不设置is_secondary
    cursor.execute("UPDATE registrations SET data = %s, create_time = %s, status = 'pending', reject_reason = NULL, priority = %s, template_uid = %s, amount = %s WHERE uid = %s", (json.dumps(data, ensure_ascii=False), now, priority, template_uid, amount, uid))
```

## 总结

本修改方案专注于解决图片中橙色头像人物提出的核心问题：**只有当员工处理过订单后，用户修改才应该标记为二次订单**。修改范围小、风险低、向后兼容，能够精准解决当前系统的逻辑问题。