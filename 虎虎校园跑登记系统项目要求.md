# 虎虎校园跑登记系统项目要求

## 简介

本系统主要用于位用户提供校园跑个人信息填写、工单问题反馈与处理的功能。

## 技术栈

前端基于Vue开发

后端基于Python FastAPI开发

数据库使用MySQL

## 规范

### 前端界面规范

1. 技术要求：Vue3
2. 组件库：Vuetify
3. 界面支持响应式尺寸变化，手机端与电脑端内容无错位正常显示与使用。
4. 使用**PWA** Vite Plugin插件被浏览器推荐打包为APP
5. 所有接口地址统一写在一个文件中如：`config/api-url.ts`，使用ts枚举定义每个接口的绝对地址，导出此enum。
6. 所有接口返回类型与请求体类型都写在一个文件内如：`config/api-type.ts`，并且导出所有类型。
7. 无论使用fetch还是xhr都需要封装一个完整的请求函数，这个函数支持类型参数填写导出的响应体类型和请求体类型，保证Ts可以正确显示类型提示。
8. views目录用来存储页面，要与访问路径相对应。每个索引页面使用index.vue。
9. 界面设计简洁规范，使用主流配色，搭配左右分栏模式。

### 后端规范

#### 返回结果规范

```json
{
  code: 200,
  msg: "successfully send",
  data:[]
}
```

每个接口的返回结果中都必须包含：

| 名称 | 作用                         | 类型            | 是否必须包含 |
| ---- | ---------------------------- | --------------- | ------------ |
| code | 状态码，与HTTP响应码一致     | Int             | 是           |
| msg  | 返回状态信息，只允许使用英文 | String          | 是           |
| data | 返回数据                     | any[] \| object | 是           |

#### 数据库

使用MySQL数据库

**账户数据**

1. 账户唯一UID（注意并非替换原ID字段），使用token的标准格式生成唯一UID。
2. 创建新账户时用户名可重复，密码要求最少6位只能包含数字、字母和短横线（不需要同时包含，也可以都不包含但是不允许包含超过他们的类型）。
3. 记录内容应包含：用户名、密码（SHA256 / MD5）、添加时间、上次登录时间、上次登录设备、登录会话tokens。
4. 存储账户登录会话时只能存储数据库中登录会话token，而非用户名或用户UID。若token被移除则登录也对应失效。

**跑步单子数据**

1. 跑步软件
2. 学校
3. 联系方式等

> [!NOTE]
>
> 与下方跑步申请信息表单填写包含的内容一致。

## 功能

1. 构建一套完整的账户系统，包含普通账户与管理员账户

   1. 普通账户无法注册，需要通过后台管理员创建账户，普通账户只可用来登录
   2. 管理员账户系统中默认存在一个管理员账户，管理员拥有管理所有账户的权限：包含删除账户、创建账户

2. 跑步申请信息表单填写，包含的内容：

   | 命题                         | 类型                  | 必填 | 其他要求                  |
   | ---------------------------- | --------------------- | ---- | ------------------------- |
   | 跑步时人脸识别               | boolean               | 是   |                           |
   | 跑步用的APP                  | 运动世界校园          | 是   | 列表，等待后台录入        |
   | 学校名称                     | 哈尔滨信息工程学院    | 是   | 列表，等待后台录入        |
   | 校区                         | string                | 否   | 用户自行填写              |
   | 学校是第一次开展校园跑吗     | boolean               | 是   |                           |
   | 手机号                       | string                | 是   |                           |
   | 密码                         | string                | 是   |                           |
   | 一天跑步次数区间             | int - int             | 是   | 使用区间，分别是最低-最高 |
   | 学校规定的单次最低下限公里数 | float                 | 是   |                           |
   | 学校规定的单次最高上限公里数 | float                 | 是   |                           |
   | 下单公里数需求               | float                 | 是   |                           |
   | 跑步准确的时间段             | time - time           | 否   | 使用区间，时间区间。      |
   | QQ号                         | string                | 是   | 记得包含正则验证          |
   | 微信号                       | string                | 是   |                           |
   | 备注                         | string (Length:0-500) | 否   |                           |

   > [!NOTE]
   >
   > 此表单要支持在后台管理员动态修改内容，如列表项需要支持动态修改以及表单项的增删。
   > 注意此表单可能还要在某些部分添加一些小提示，所以后台需要复杂的动态表单修改逻辑功能。

3. 工单申诉功能，用户自行填写反馈信息。反馈内容长度在0-100个字符之间。必须登录后才能使用，管理员可以处理反馈信息，处理完成后普通用户端显示已处理。

4. 后台支持对用户增删改查，支持创建普通账户与管理员账户。