# 2026年6月4日更新实施方案

1. 在模板配置功能窗口中加入自行配置加粗、字体颜色、字体大小功能。配置时字段名和字段值一同配置。与自定义显示复制按钮类似，主要用于订单信息预览时显示样式。不影响填写模板的样式。
2. 新增公告功能，公告会在用户进入网站后以弹窗的形式显示。显示当前的最新公告，其次在左侧边栏加入“公告”功能区，可以显示所有公告。在权限管理中加入“编辑公告”/“发布公告”/“删除公告”/“查看公告”权限。无查看公告权限的用户不支持查看历史公告但是最新公告依然会提示。样式上使用时间线排版样式（注意移动端排版）。超级管理员默认全部包含权限，记得为新增的权限勾选上。
3. 订单处理的表格中给数量数据文本改为红色、最粗。
4. 订单处理的订单详情界面中，使用范围选择器编辑的值会使用逗号间隔，改为使用短横线。
5. 订单信息修改功能：对于未处理的订单允许分字段修改字段值，在订单详情上方加入“修改”按钮，弹出新的窗口编辑每个字段的字段值，并且使用类似“驳回”重新编辑窗口的编辑模式。需要包含旧的表单值。如若发现旧的表单值与该模板不一致，则弹出警告“无法恢复原状态，模板与原模板不一致”，不恢复旧的填写值。
6. 订单新建功能区的历史订单加入“修改”功能，或理解为不再是驳回才可修改，修改后同样将订单标记为未处理。但是注意，里面要包含旧的表单值，不要新起模板重新填写表单值。如若发现旧的表单值与该模板不一致，则弹出警告“无法恢复原状态，模板与原模板不一致”，不恢复旧的填写值。
7. 订单详情的聊天界面无法显示自己发送的气泡的消息时间，改为支持显示。
8. 发现版本更新时下方弹出的版本更新提示不够强制，发现版本更新后禁止用户访问其他页面，页面上显示一个白页面提示发现新版本，需要更新版本。