# 如何启动

第一步 启动Docker

第二步 在 `app`目录下启动开发服务器

```bash
npm run dev
```

第三步 运行Docker compose 容器

```bash
docker compose up -d
```

不出意外的话访问

http://localhost:3000 即可

> [!IMPORTANT]
>
> 每次部署都需要修改在`server/src/account_service.py`的数据库端口号、密码、用户名。

