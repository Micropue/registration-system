# 虎虎校园跑登记系统 — 部署与启动方法

## 系统架构

| 组件 | 技术栈 | 端口 |
|------|--------|------|
| 前端 | Vue 3 + Vite + Vuetify 4 | `3000` |
| 后端 API | Python FastAPI + Uvicorn | `8001` |
| WebSocket 聊天 | FastAPI WebSocket | `8001` (同端口) |
| 数据库 | MySQL 8.0 (Docker) | `3308` → `3306` |

---

## 一、环境要求

- Node.js 18+
- Docker & Docker Compose
- Python 3.10+ (仅本地开发需要)

---

## 二、启动步骤

### 1. 启动 Docker

确保 Docker Desktop 或 Docker Engine 正在运行。

### 2. 启动数据库和后端容器

```bash
docker compose up -d
```

这会启动：
- `huhurun-database` — MySQL 8.0 数据库（端口 3308）
- `huhurun-api` — FastAPI 后端服务（端口 8001）

首次启动会自动创建数据库和表结构（包括 `users`、`registrations`、`feedbacks`、`registration_chats` 等）。

### 3. 启动前端开发服务器

```bash
cd app
npm install   # 首次运行
npm run dev
```

前端默认运行在 `http://localhost:3000`，API 请求通过 Vite 代理转发到后端 `8001` 端口。

---

## 三、访问地址

| 地址 | 说明 |
|------|------|
| `http://localhost:3000` | 用户端 / 管理端 |
| `http://localhost:8001/docs` | FastAPI 接口文档 (Swagger) |
| `ws://localhost:8001/ws/chat/{id}` | WebSocket 聊天 |

默认管理员账户：`admin` / `admin-123456`

---

## 四、配置说明

数据库连接在 `docker-compose.yml` 中通过环境变量配置：

```yaml
environment:
  - DB_HOST=db           # 容器内服务名
  - DB_PORT=3306
  - DB_NAME=huhurun
  - DB_USERNAME=root
  - DB_PASSWORD=root
```

本地开发时，环境变量可从 `server/src/account_service.py` 的 `__init__` 方法读取，默认值：

```python
host = "localhost"
port = 3308
database = "huhurun"
user = "root"
password = "root"
```

---

## 五、前端代理配置

`app/vite.config.mts` 配置了 API 和 WebSocket 代理：

```typescript
proxy: {
  '/api': {
    target: 'http://localhost:8001/',
    changeOrigin: true,
    rewrite: (p) => p.replace(/^\/api/, '')  // 去掉 /api 前缀
  },
  '/ws': {
    target: 'ws://localhost:8001',
    ws: true,           // 启用 WebSocket 代理
    changeOrigin: true
  }
}
```

---

## 六、常用命令

```bash
# 重启后端（代码修改后）
docker compose restart server

# 查看后端日志
docker compose logs -f server

# 完全重建（清数据）
docker compose down -v && docker compose up -d

# 后端进入容器调试
docker exec -it huhurun-api bash
```

---

## 七、数据表一览

| 表名 | 用途 |
|------|------|
| `users` | 用户账户 |
| `login_sessions` | 登录会话 |
| `registrations` | 登记记录（含 JSON 字段数据） |
| `registration_fields` | 登记表单字段配置 |
| `running_apps` | 跑步 APP 配置（名称/价格/强调色） |
| `feedbacks` | 工单反馈 |
| `feedback_replies` | 工单回复 |
| `notifications` | 系统通知 |
| `registration_chats` | 登记聊天记录（WebSocket 实时通信） |

---

> [!IMPORTANT]
> 每次部署都需要确认 `server/src/account_service.py` 中的数据库端口号、密码、用户名与 `docker-compose.yml` 一致。
