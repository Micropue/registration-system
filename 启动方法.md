# 登记系统 — 部署与运维手册

> 从零开始部署请先看 **[README.md](README.md)** 的「生产部署」章节；本文档聚焦环境变量、服务管理、数据库、备份与排障。

---

## 1. 系统架构与端口

```
浏览器（PWA）
   │  HTTPS
   ▼
Nginx ──┬── /            静态前端（app/dist）
        ├── /api/**      → FastAPI（重写去掉 /api 前缀）
        ├── /media/**    → FastAPI 静态资源（上传的图片）
        └── /ws/**       → FastAPI WebSocket（通知 / 订单聊天 / 聊天室）
                            │
                            ▼
                        MySQL 8.0
```

| 组件 | 技术栈 | 开发端口 | 容器内 / 生产端口 |
| --- | --- | --- | --- |
| 前端开发服务器 | Vue 3 + Vite 8 + Vuetify 4 | `3000`（被占用自动 +1） | — |
| 前端静态资源 | `app/dist`（Nginx 托管） | — | 443（HTTPS） |
| 后端 API | FastAPI + Uvicorn | `8001` → 容器 `8000` | `8000` |
| WebSocket | FastAPI WebSocket | `8001`（同端口） | `8000`（同端口） |
| 数据库 | MySQL 8.0 | `3308` → 容器 `3306` | `3306` |

Docker 容器与卷：

| 名称 | 说明 |
| --- | --- |
| `huhurun-api`（compose 服务名 `server`） | 后端容器，挂载 `./server/src` |
| `huhurun-database`（compose 服务名 `db`） | MySQL 容器 |
| `huhurun-server_db-data` | 数据库数据卷 |
| `huhurun-server_api-net` | 内部网络 |

---

## 2. 环境要求

| 组件 | 版本 | 用途 |
| --- | --- | --- |
| Docker / Docker Compose | 20+ | 运行 MySQL 与后端 |
| Node.js | 20.19+ 或 22.12+（CI 使用 22） | 前端构建 |
| Python | 3.10+ | 仅在宿主机直接运行后端时需要 |
| Nginx | 任意稳定版 | 生产反向代理（可选） |

后端依赖仅 4 个（`server/requirements.txt`）：`fastapi[standard]`、`mysql-connector-python`、`openpyxl`、`python-multipart`。

---

## 3. 本地开发启动

```bash
# 1. 启动数据库 + 后端
docker compose up -d

# 2. 启动前端
cd app
npm install
npm run dev
```

访问 `http://localhost:3000`，使用默认管理员 `admin` / `admin-123456` 登录。

后端源码目录 `./server/src` 以卷挂载进容器，Uvicorn 处于 `--reload` 模式，改动 Python 文件会自动重载；如未生效：

```bash
docker compose restart server
```

---

## 4. 环境变量参考

### 4.1 后端

| 变量 | 说明 | Compose 值 | 代码兜底值 |
| --- | --- | --- | --- |
| `DB_URL` | `host:port` 形式地址，支持 `mysql://` 前缀 | `db:3306` | — |
| `DB_HOST` | 主机（优先于 `DB_URL`） | — | `localhost` |
| `DB_PORT` | 端口 | — | `3308` |
| `DB_NAME` | 数据库名 | `huhurun` | `huhurun` |
| `DB_USERNAME` | 用户名 | `root` | `root` |
| `DB_PASSWORD` | 密码 | `root` | `root` |
| `MEDIA_DIR` | 上传文件目录 | `/data/media` | `server/media` |
| `TZ` | 时区 | `Asia/Shanghai` | 系统时区 |

- 支持 `.env` 文件（`python-dotenv`，在 `server/src/database.py` 中加载），`.env` 已被 `.gitignore` 忽略。
- `server/src/database.py` 顶部的 `_FALLBACK_*` 常量用于非 Docker 场景，**被标记为禁止修改**；容器内以环境变量优先。

### 4.2 前端

前端无环境变量，代理与 PWA 配置在 `app/vite.config.mts`：

| 路径 | 开发目标 | 说明 |
| --- | --- | --- |
| `/api` | `http://localhost:8001` | `rewrite` 去掉 `/api` 前缀 |
| `/media` | `http://localhost:8001` | 上传图片 |
| `/ws` | `ws://localhost:8001` | WebSocket |

> `npm run build` 时会临时把 8001 改成 8000（生产后端端口）再构建，完成后自动还原；开发端口始终是 8001。

### 4.3 后端限制常量（`server/src/main.py`）

| 常量 | 值 | 说明 |
| --- | --- | --- |
| `ALLOWED_ORIGINS` | `localhost:3000`、`localhost:3001`、生产域名 | CORS 白名单，新增域名需同步维护 |
| `MAX_IMAGE_SIZE` | 1MB | 图片上传上限 |
| 允许类型 | PNG / JPEG / WebP / GIF / HEIC / HEIF | 同时校验 MIME、扩展名、文件头 |

---

## 5. 生产部署要点

完整步骤（Docker Compose、Nginx 示例、CI/CD、上线检查）见 **[README.md — 生产部署](README.md#生产部署)**，这里只列容易踩坑的要点：

1. **Nginx 的 `/api/` 必须去掉前缀**再转发（`rewrite ^/api/?(.*)$ /$1 break;`），与开发代理行为一致。
2. **`/ws/` 必须透传** `Upgrade` / `Connection` 头，且 `proxy_read_timeout` 设置足够大（聊天长连接）。
3. **PWA 需要 HTTPS**，否则无法安装、Service Worker 不生效。
4. **`client_max_body_size`** 需大于 1MB（图片上传上限），示例配置为 10m。
5. **生产运行模式**：`server/Dockerfile` 默认 `fastapi dev`（热重载），生产建议改为 `fastapi run` 或 `uvicorn main:app --workers N`。
6. **前后端需同时发布**：接口路径或登记数据结构变更后，旧版前端（尤其 PWA 缓存）可能调用失败；应用会在检测到新版本时强制更新。
7. **GitHub Actions CD 当前为手动触发**（自动部署已关闭）：在工作流文件中 `push` 触发被注释，需在 Actions 页面 Run workflow，或在需要时恢复注释。Secrets 仅需 `SERVER_HOST`、`SERVER_USER`、`SERVER_KEY`；部署目标为 `/www/huhurun-www/client`（前端 dist）与 `/sdb/huhurun@api/src`（后端源码）。
8. **后端依赖变更**（修改 `requirements.txt`）不会被 CD 同步，需要在服务器重新构建/安装。

---

## 6. 服务管理常用命令

```bash
# 启动 / 停止 / 重建
docker compose up -d                 # 启动（后台）
docker compose up -d --build         # 重新构建镜像后启动
docker compose restart server        # 仅重启后端
docker compose down                  # 停止（保留数据卷）
docker compose down -v               # 停止并删除数据卷（清空数据库，慎用）

# 状态与日志
docker compose ps
docker compose logs -f server        # 后端日志
docker compose logs -f db            # 数据库日志
docker compose logs --tail=100 server

# 进入容器
docker exec -it huhurun-api bash
docker exec -it huhurun-database mysql -uroot -proot huhurun
```

前端：

```bash
cd app
npm install          # 安装依赖
npm run dev          # 开发（3000，占用则 +1）
npm run type-check   # vue-tsc 类型检查
npm run build        # 生产构建（含类型检查、更新日志生成）
npm run preview      # 本地预览 dist
```

---

## 7. 数据表结构

数据库与所有表在后端启动时自动创建/迁移（`init_db` + `ensure_super_admin_permissions`），无需手工执行 SQL。共 20 张表：

| 表名 | 用途 | 关键字段 |
| --- | --- | --- |
| `users` | 账户 | `uid`、`username`、`password`（SHA-256）、`login_sessions`(JSON)、`group_uid`、`register_time`、`last_login_*` |
| `login_sessions` | 登录会话（token） | `uid`、`token`、`user_uid`、`create_time` |
| `user_groups` | 账户组与权限树 | `uid`、`name`、`permissions`(JSON) |
| `user_subordinates` | 上级-下级关系 | `parent_uid`、`subordinate_uid` |
| `balance_delegations` | 余额扣除链接 | `user_uid`、`parent_uid`、`app_uid`（唯一约束） |
| `running_apps` | APP 配置 | `uid`、`name`、`note`、`accent_color`、`icon`、`balance_mode`、`balance_round`、`sort_order` |
| `app_templates` | APP 模板版本 | `uid`、`app_id`、`version_name`、`fields`(JSON) |
| `registration_fields` | 旧版全局表单字段（兼容） | `label`、`type`、`required`、`default_val`、`options` |
| `registrations` | 登记订单 | `uid`、`user_uid`、`data`(JSON，含 `应用`)、`status`、`priority`、`template_uid`、`amount`、`reject_reason`、`process_count`、`is_secondary` |
| `registration_chats` | 订单聊天消息 | `registration_uid`、`sender_uid`、`message`、`msg_type`、`is_recalled` |
| `global_chats` | 聊天室消息 | `sender_uid`、`message`、`msg_type`、`image_url`、`is_recalled`、`is_pinned` |
| `feedbacks` | 工单 | `uid`、`user_uid`、`title`、`content`、`status` |
| `feedback_replies` | 工单回复 | `feedback_uid`、`user_uid`、`is_admin`、`content` |
| `user_balances` | 用户 × APP 余额 | `user_uid`、`app_uid`（唯一）、`balance`、`updated_at` |
| `balance_transactions` | 余额流水 | `app_uid`、`user_uid`、`type`、`amount`、`balance_after`、`related_uid`、`note` |
| `balance_recharges` | 充值申请 | `user_uid`、`app_uid`、`amount`、`reason`、`status`、`processed_by` |
| `notifications` | 站内通知 | `user_uid`、`type`、`reference_id`、`title`、`content`、`is_read` |
| `announcements` | 公告 | `title`、`content`、`publisher_uid`、`is_published`、`update_time` |
| `daily_report_fields` | 日报字段配置 | `label`、`type`、`required`、`options` |
| `daily_reports` | 日报数据 | `user_uid`、`report_date`、`data`(JSON) |

> 部分字段通过启动时的 `ALTER TABLE ... ADD COLUMN` 迁移添加（如 `registrations.amount`、`registration_chats.is_recalled`、`global_chats.is_pinned`），属正常现象。

### 7.1 启动时自动迁移

后端每次启动会执行：

1. 建库、建表、补充缺失列（`init_db`）；
2. 超级管理员组权限树刷新为最新 `PERMISSION_TREE`（`ensure_super_admin_permissions`）；
3. 历史数据兼容迁移：把登记数据里旧字段名统一改为 `应用`（幂等，执行后不再匹配）。

---

## 8. 数据备份与恢复

```bash
# 备份（宿主机通过 3308 端口访问容器内 MySQL）
docker exec huhurun-database sh -c \
  'exec mysqldump -uroot -proot --default-character-set=utf8mb4 huhurun' > backup_$(date +%F_%H%M).sql

# 恢复（会覆盖同名表数据）
docker exec -i huhurun-database mysql -uroot -proot --default-character-set=utf8mb4 huhurun < backup_2026-01-01_0300.sql

# 定时备份示例（crontab，每天 3 点）
0 3 * * * docker exec huhurun-database sh -c 'exec mysqldump -uroot -proot --default-character-set=utf8mb4 huhurun' > /srv/backup/huhurun_$(date +\%F).sql
```

上传的图片保存在 `MEDIA_DIR`（Compose 为容器内 `/data/media`）。如需持久化到宿主机，可在 `docker-compose.yml` 中为该目录额外挂载卷并一并备份。

---

## 9. 日志与排障

### 9.1 常见问题速查

| 现象 | 可能原因与处理 |
| --- | --- |
| 启动日志 `Error initializing database: 2055: Cursor is not connected` | 既有问题：`init_db` 中 `ALTER TABLE registration_chats ADD COLUMN msg_type` 在列已存在时抛错，`except` 捕获后底层游标被断开，导致其后的建表语句不再执行。若所有表已存在则不影响使用；如需彻底修复，把该 `ALTER` 改为与其他迁移一致的 `SHOW COLUMNS ... LIKE` 先判断再执行 |
| 前端接口 502 / 404 | 后端未启动（`docker compose ps`）；Nginx `/api/` 未去掉前缀 |
| 登录返回 401 且提示状态失效 | token 过期（7 天）或被强制下线，重新登录 |
| 数据库连接失败 | 容器未就绪 / `.env` 或 compose 中 `DB_*` 不一致；注意宿主机端口 `3308`、容器内 `3306` |
| 图片上传 403 | `Origin`/`Referer` 不在白名单，或 `User-Agent` 命中自动化工具黑名单（curl、wget、axios 等） |
| 图片上传 413 / 报错超过 1MB | 后端 `MAX_IMAGE_SIZE=1MB`；Nginx `client_max_body_size` 需同步放大 |
| WebSocket 连接失败 / 频繁断开 | Nginx 未透传 `Upgrade`；`proxy_read_timeout` 过短；HTTPS 站点需用 `wss://` |
| 更换域名后跨域报错 | 把新域名加入 `server/src/main.py` 的 `ALLOWED_ORIGINS` 并重启后端 |
| PWA 一直提示更新 | 点击「立即更新」会强制刷新；仍异常则清除站点数据（含 Service Worker 缓存） |
| 修改了提交历史但更新日志没变 | 更新日志由构建脚本从 `git log` 生成，需重新 `npm run build`；不要手工编辑 `app/src/config/update-logs.ts` |

### 9.2 常用诊断命令

```bash
# 后端健康检查（公开接口）
curl -s http://localhost:8001/apps

# 直接查数据库
docker exec -it huhurun-database mysql -uroot -proot --default-character-set=utf8mb4 huhurun \
  -e "SELECT COUNT(*) FROM registrations;"

# 查看后端最近日志
docker compose logs --tail=200 server
```

---

## 10. 发布检查清单

- [ ] `cd app && npm run type-check && npm run build` 本地通过
- [ ] 后端 `python3 -m py_compile server/src/main.py server/src/account_service.py` 通过
- [ ] 涉及接口/数据结构变更时，前后端同一次发布
- [ ] push 到 `main` 后确认两个 GitHub Actions 工作流成功
- [ ] 重启服务器后端进程（容器：`docker compose restart server`；其他托管方式：重启对应服务）
- [ ] 打开线上页面走一遍登录 → 新建订单 → 管理员处理 主流程
- [ ] 发布前执行一次数据库备份（见第 8 节）
